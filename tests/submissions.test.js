module.exports = (app, request ) => {
    describe('Submissions', () => {

        it('Can get details of submission', done => {
            fail()
            done()
        })

        it('Can create submission for form', done => {
            fail()
            done()
        })

        it('Can delete existing submission', done => {
            fail()
            done()
        })

    })
}