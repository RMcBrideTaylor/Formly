module.exports = (app, request ) => {
    describe('Form', () => {

        it('can create new form with inputs', done => {
            fail()
            done()
        })

        it('can give group access to form', done => {
            fail()
            done()
        })

        it('can update inputs on exisiting form', done => {
            fail()
            done()
        })

        it('can delete form and inputs', done => {
            fail()
            done()
        })

    })
}