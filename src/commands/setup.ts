import Client from '../models/client'

export default function (args : string[] ) {
  // tslint:disable-next-line:no-console
  console.log(args)

  // Set up the app key if necessary


  // Generate initial client for front end
}
